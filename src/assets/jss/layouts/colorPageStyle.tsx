import { Theme } from "@mui/material";
const colorPageStyle = (theme: Theme) => ({
  paper: {
    padding: 20,
    display: "flex",
    flexDirection: "column",
    gap: 50,
    marginTop: 30,
  },
});

export default colorPageStyle;
